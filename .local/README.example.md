# Personal local wrappers

Files under `.local/` are ignored except for this example. Put machine-specific
launchers, convenience commands, or local notes here rather than adding them to
the tracked `scripts/local/` directory.

Do not store secrets, credentials, databases, uploaded documents, or migration
reports in this directory.
